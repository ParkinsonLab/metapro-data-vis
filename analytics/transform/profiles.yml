rpkm_transform:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Fallback only; run_pipeline.py always sets DBT_DUCKDB_PATH.
      path: "{{ env_var('DBT_DUCKDB_PATH', '../local-data/vis/runs/default/sample.duckdb') }}"
      module_paths:
        - "transform"
